{
  "name": "hrms",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --legacy-peer-deps && npm run build",
    "start": "cd backend && npm start"
  }
}
